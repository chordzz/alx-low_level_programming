#include "main.h"
#include <stdlib.h>

/**
 * array_range - creates an array of integers
 * @min: integer
 * @max: integer
 * Return: pointer
 */
int *array_range(int min, int max)
{
	if (min > max)
		return (NULL);


}
